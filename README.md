# Home Assistant Apps

An independent collection of Home Assistant apps. Each app lives in its own
directory under `apps/` and can be versioned and released independently.

## Included apps

| App | Purpose |
| --- | --- |
| [`rdp-gateway`](apps/rdp-gateway) | Browser-based RDP access embedded in Home Assistant |
